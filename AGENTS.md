## Agent skills

### 问题跟踪

本仓库使用 GitHub Issues 跟踪 Issue 和 PRD；外部 PR 不纳入分诊。见 `docs/agents/issue-tracker.md`。

### 分诊标签

使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix`。见 `docs/agents/triage-labels.md`。

### 领域文档

使用单一上下文布局。见 `docs/agents/domain.md`。
