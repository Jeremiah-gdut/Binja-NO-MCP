# 按证据表达引用边

Binary Ninja 的分析引用可能缺失，因此导出不能把它们当作完整调用图。关系边必须保留证据来源：`bn_analysis` 用于导航提示，`direct_il` 表示 IL 中可直接识别的目标，`unresolved_indirect` 表示存在间接调用但目标未知。调用证据存放在源函数 meta，函数索引不承载调用图，且不额外生成全局 xref 文件。第一版只补充可验证直接调用，不猜测恢复间接目标；这以更少的召回率换取 Agent 不会把推测边当作事实。
